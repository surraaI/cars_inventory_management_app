<%- include('../layout') %>
<h1>Categories</h1>
<% categories.forEach(category => { %>
    <div>
      <h2><a href="/cars/<%= category.id %>"><%= category.name %></a></h2>
      <p><%= category.description %></p>
    </div>
  <% }); %>
  <a href="/categories/new">Add New Category</a>
  